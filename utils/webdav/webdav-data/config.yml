# Server settings
address: 0.0.0.0
port: 8001
auth: true
tls: false
cert: cert.pem
key: key.pem
prefix: /
debug: false
noSniff: false
behindProxy: false

# Default user settings
directory: .
permissions: R
rules: []
rulesBehavior: overwrite

log:
  format: console
  colors: true
  outputs:
  - stderr

#cors:
#  enabled: true
#  credentials: true
#  allowed_headers:
#    - Depth
#  allowed_hosts:
#    - http://localhost:8080
#  allowed_methods:
#    - GET
#  exposed_headers:
#    - Content-Length
#    - Content-Range

users:
#  - username: admin
#    password: admin
  - username: "{env}USERNAME"
    password: "{env}PASSWORD"
    directory: /data
    permissions: CRUD
